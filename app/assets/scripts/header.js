/*global header*/
'use strict';

header.controller('HeaderController', [
    '$scope',
    function (
        $scope
    ) {

        /*var iconData = [
            {name: 'bars'}
        ];

        $scope.fonts = [].concat(iconData);*/
    }
]);
